import { StyleSheet } from "react-native";
import { Color } from "../../constant/Color";

export const LoginS = StyleSheet.create({
  writezone: {
    backgroundColor: Color.backgroundWriteText,
    height: "35%",
    borderRadius: 20,
    alignItems: "center",
    justifyContent: "space-evenly",
  },
});
